.DS_Store
*.log
node_modules/
.vscode/
.idea/

# Lighthouse report files are regenerated on every CI run.
# We commit AUDIT.md (the human-written findings) + the helper scripts,
# but not the raw HTML/JSON reports — they bloat git history.
audit/*.report.html
audit/*.report.json
